# 룰: 장소 수집 및 구글 지도 분류 저장 (Place Ingestion Rule)

## 📌 목적
사용자가 유튜브 링크, 네이버 블로그, 티스토리 등 블로그, 혹은 **인스타그램(릴스, 피드 게시물, 캐러셀 카드뉴스)** 등 여행 관련 콘텐츠 링크를 제시하거나 관련 텍스트를 전달했을 때, 콘텐츠 속의 핵심 장소들을 자동으로 추출·분류하여 프로젝트의 구글 지도 저장 데이터베이스(`data/places/places_master.json`) 및 구글 내 지도 CSV(`data/mymaps/`)에 저장하고 사용자에게 안내합니다.

## 🎯 트리거 조건
- 유튜브 URL (`youtube.com`, `youtu.be`) 포함 메시지
- 블로그 URL (`blog.naver.com`, `tistory.com`, `brunch.co.kr` 등) 포함 메시지
- 인스타그램 URL (`instagram.com/reel/`, `instagram.com/p/`, `instagram.com/reels/`) 포함 메시지
- "이 링크에 있는 장소 저장해줘", "이 인스타 맛집/카페 저장해줘", "이 유튜브 맛집들 구글지도에 넣어줘", "이 블로그 명소들 저장해줘" 등의 요청

## 🛠️ 수행 절차
1. **웹페이지/콘텐츠 열람 및 추출**:
   - 일반 웹/블로그: 에이전트 도구 `read_url_content`를 호출하여 본문 텍스트, 장소 상호명, 주소, 특징을 파악합니다.
   - 인스타그램 링크: 임베드 캡션 엔드포인트(`/embed/captioned/`) 및 소셜 크롤러 User-Agent(`facebookexternalhit/1.1`)를 활용하여 캡션 속 핀(`📍`, `📌`, `🚩`, `🏩`, `🏛️`, `☕`, `🍽️`), 주소, 영업시간, 캐러셀 이미지 슬라이드를 추출합니다.
   - 이미지 카드 뉴스/캐러셀 형태의 인스타그램 게시물은 슬라이드 이미지에 표기된 장소명(예: `📍 가조엔 도쿄 1층 카페`, `📍 세타가야 미술관` 등)을 시각적으로 판별하여 정확한 장소 데이터로 추출합니다.
   - 파이썬 모듈 `src/place_extractor.py` 또는 `python3 cli.py add-link <URL>`를 실행하여 자동 추출 및 저장을 수행합니다.
2. **장소 식별 및 6대 카테고리 자동 분류**:
   - `식당 및 카페`: 음식점, 카페, 베이커리, 타파스 바, 비스트로, 브런치 등
   - `숙박`: 호텔, 호스텔, 리조트, 에어비앤비, 료칸, 민박 등
   - `유적지 및 관광지`: 성당, 궁전, 박물관, 미술관, 랜드마크, 전망대, 스튜디오 등
   - `자연 및 공원`: 공원, 해변(비치), 호수, 산, 산책로 등
   - `쇼핑 및 시장`: 전통시장, 야시장, 소품샵, 백화점, 아울렛 등
   - `기타`
3. **브라우저 자동 저장 금지 및 지역별 모듈화 저장 원칙**:
   - 🚫 **브라우저 구글 지도 저장 자동화 전면 금지**: 웹 브라우저를 띄워 구글 지도 웹 UI를 클릭하며 '저장'하는 행위는 불안정하고 느리므로 절대 시도하지 않습니다.
   - 📌 **지역별 폴더 분리 및 5대 카테고리 동기화 (Region-based Modular Architecture)**:
     - 구글 내 지도는 개별 여행(후쿠오카, 스페인 등 도시/국가 단위)별로 독립된 지도를 생성하므로, 장소들이 전 세계 통합 파일에만 섞여 있으면 여행 계획 시 다른 지역 핀들이 함께 표시되어 지도 뷰 왜곡과 혼란을 줍니다.
     - 모든 신규 수집 장소는 마스터 DB(`data/places/places_master.json`)에 영구 저장됨과 동시에, `data/mymaps/{지역명}/` 폴더에 해당 지역의 5대 카테고리 CSV(`내지도_*.csv`) 및 단일 레이어용 `{지역명}_전체.csv`로 자동 동기화됩니다.
     - 전 세계 통합본도 `data/mymaps/_전체_통합/` 및 루트 `data/mymaps/`에 함께 유지됩니다.
   - 🗺️ **구글 내 지도(My Maps) 레이어 1:1 최적화**:
     - 구글 내 지도(My Maps)에서 특정 여행지를 계획할 때 해당 지역 폴더(`data/mymaps/<지역>/`)의 카테고리별 CSV를 업로드하면 해당 지역 장소만 완벽하게 시각화됩니다.
     - `검색위치` 열을 마커 배치 위치로 지정하여 오인 없는 핀 배치를 보장합니다.
4. **저장소 자동 반영**:
   - `src.storage.add_place`를 통해 중복 여부를 체크하며 `data/places/places_master.json`에 영구 저장합니다.
   - `data/mymaps/<지역>/` 및 `data/mymaps/_전체_통합/`의 CSV 파일들이 자동으로 최신화되어 누적됩니다.
5. **원격 저장소 자동 푸시 (Remote Git Auto-Push by Default)**:
   - 데이터 저장 직후 `src.git_sync.git_auto_push` 또는 `git push origin main`을 **디폴트로 자동 실행**하여 깃허브 원격 저장소와 100% 실시간 동기화합니다.
6. **사용자 응답 형식**:
   - 추출된 장소 개수와 카테고리별 분류 결과 요약
   - 장소별 클릭 가능한 [구글 지도 상세/검색 링크](file:///) 제공
   - 지역별 폴더(`data/mymaps/<지역>/`), 전체 통합 CSV, 및 원격 깃허브 푸시 완료 현황 안내
